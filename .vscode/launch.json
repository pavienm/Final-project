{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run & Debug (g++)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/fare",
      "cwd": "${workspaceFolder}",
      "args": [],
      "preLaunchTask": "Build (g++)",
      "MIMode": "gdb",
      "miDebuggerPath": "gdb"
    },
    {
      "name": "Run & Debug (MSVC)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\fare.exe",
      "cwd": "${workspaceFolder}",
      "args": [],
      "preLaunchTask": "Build (MSVC)"
    }
  ]
}
